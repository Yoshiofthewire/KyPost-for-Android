<?xml version="1.0" encoding="utf-8"?>
<!-- app/src/main/res/layout/activity_contact_detail.xml -->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/contactDetailRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/contactDetailAvatar"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="12dp"
            android:textSize="22sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/contactDetailName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal"
            android:textSize="22sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/contactDetailSubtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:gravity="center_horizontal"
            android:textSize="15sp"
            android:visibility="gone" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginTop="12dp">

            <com.google.android.material.chip.Chip
                android:id="@+id/contactDetailSelfBadge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:visibility="gone"
                app:chipMinHeight="24dp"
                app:ensureMinTouchTargetSize="false"
                xmlns:app="http://schemas.android.com/apk/res-auto" />

            <com.google.android.material.chip.Chip
                android:id="@+id/contactDetailPgpBadge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:visibility="gone"
                app:chipMinHeight="24dp"
                app:ensureMinTouchTargetSize="false"
                xmlns:app="http://schemas.android.com/apk/res-auto" />

        </LinearLayout>

        <!-- Populated programmatically in ContactDetailActivity.render() — one section header
             (row_contact_detail_header) plus one row (row_contact_detail_row) per populated field,
             skipping any field/section the contact has nothing in. -->
        <LinearLayout
            android:id="@+id/contactDetailFieldsContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginTop="8dp" />

    </LinearLayout>

</ScrollView>
